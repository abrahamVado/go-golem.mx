# API

Base prefix: `/api/v1`. Public: health, register, login, refresh, recover, reset-password. Private: logout, dashboard, me, users, roles, permissions, settings, companies/current, branches, audit-logs.
